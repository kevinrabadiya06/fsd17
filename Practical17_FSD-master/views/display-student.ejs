<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tution Admin Panel</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f4f6f9;
      margin: 0;
      padding: 20px;
    }

    h1 {
      text-align: center;
      color: #333;
    }

    a {
      text-decoration: none;
      padding: 8px 15px;
      border-radius: 5px;
      font-size: 14px;
      margin: 3px;
      display: inline-block;
    }

    /* Add Student button */
    .add-btn {
      background: #28a745;
      color: white;
      margin-bottom: 15px;
      display: inline-block;
    }
    .add-btn:hover {
      background: #218838;
    }

    /* Action buttons */
    .edit-btn {
      background: #007bff;
      color: white;
    }
    .edit-btn:hover {
      background: #0056b3;
    }

    .delete-btn {
      background: #dc3545;
      color: white;
    }
    .delete-btn:hover {
      background: #c82333;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 15px;
      background: white;
      border-radius: 8px;
      overflow: hidden;
      box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    }

    th, td {
      padding: 12px;
      text-align: center;
      border-bottom: 1px solid #ddd;
    }

    th {
      background: #007bff;
      color: white;
    }

    tr:hover {
      background: #f1f1f1;
    }

    td a {
      font-weight: bold;
    }
  </style>
</head>
<body>
  <h1>Students List</h1>
  <a href="/add-student" class="add-btn">➕ Add Student</a>
  
  <table>
    <tr>
      <th>No</th>
      <th>Name</th>
      <th>Class</th>
      <th>Age</th>
      <th>Contact</th>
      <th>Action</th>
    </tr>
    <% for(var i=0; i<mydata.length; i++) { %>
      <tr>
        <td><%= i+1 %></td>
        <td><%= mydata[i].name %></td>
        <td><%= mydata[i].class %></td>
        <td><%= mydata[i].age %></td>
        <td><%= mydata[i].contact %></td>
        <td>
          <a href="/edit-student/<%= mydata[i]._id %>" class="edit-btn">✏️ Edit</a>
          <a href="/delete-student/<%= mydata[i]._id %>" class="delete-btn">🗑️ Delete</a>
        </td>
      </tr>
    <% } %>
  </table>
</body>
</html>
